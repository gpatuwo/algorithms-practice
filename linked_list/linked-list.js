/*
linked list as series of nodes that keep track of
what's in front (and behind if doubly-linked) of them

node as object that keeps track of
it's own data and a pointer to the next node
 */

class LinkedList {
  constructor(){
    this.head = null;
    this.tail = null;
    this.count = 0;
  }

  // add to back: assign tail's next to be this node, reasign former tail's pointers

  // add to front: 

  // remove from front

  // remove from back
}
